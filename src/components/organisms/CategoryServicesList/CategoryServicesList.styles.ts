import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    paddingTop: 20,
  },
  servicesContainer: {
    paddingBottom: 130,
    paddingHorizontal: 20,
  },
});
