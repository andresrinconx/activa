import { StyleSheet } from 'react-native';
import { charcoalGray } from '../../../theme';

export default StyleSheet.create({
  listContainer: {
    paddingHorizontal: 25,
  },
  txtDate: {
    color: charcoalGray,
    fontSize: 16,
    textAlign: 'center',
  },
});
