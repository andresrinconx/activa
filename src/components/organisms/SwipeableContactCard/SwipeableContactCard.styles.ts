import { StyleSheet } from 'react-native';
import { vibrantRed } from '../../../theme';

export default StyleSheet.create({
  btnDeleteContainer: {
    backgroundColor: vibrantRed,
    borderBottomLeftRadius: 16,
    borderTopLeftRadius: 16,
    marginLeft: -12,
  },
});
