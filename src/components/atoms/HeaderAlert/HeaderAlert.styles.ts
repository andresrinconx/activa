import { StyleSheet } from 'react-native';
import { springLime } from '../../../theme';

export default StyleSheet.create({
  container: {
    alignItems: 'center',
    backgroundColor: springLime,
    flexDirection: 'row',
    height: 53,
    padding: 16,
  },
  txt: {
    marginLeft: 10,
  },
});
